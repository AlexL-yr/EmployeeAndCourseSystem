<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.ClazzMapper">
    <select id="list" parameterType="com.itheima.pojo.ClazzQueryParam" resultType="com.itheima.pojo.Clazz">
        SELECT
        c.*,
        e.name AS masterName,
        CASE
        WHEN c.begin_date > CURDATE() THEN '未开班'
        WHEN c.end_date &lt; CURDATE() THEN '已结课'
        ELSE '在读'
        END AS status
        FROM
        clazz c
        LEFT JOIN emp e ON c.master_id = e.id
        <where>
            <if test="name != null and name != ''">
                AND c.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="begin != null and end != null">
                AND c.begin_date BETWEEN #{begin} AND #{end}
            </if>
        </where>
        ORDER BY c.update_time DESC
    </select>
    <delete id="deleteById">
        delete from clazz where id in
        <foreach collection="id" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>
    <update id="update">
        update clazz
        <set>
        <if test="name!=null and name!=''"> name = #{name},</if>
            <if test="room!=null and room!=''"> room =#{room},</if>
            <if test="beginDate!=null"> begin_date =#{beginDate},</if>
            <if test="endDate!=null "> end_date =#{endDate},</if>
            <if test="masterId!=null"> master_id =#{masterId},</if>
            <if test="subject!=null"> subject =#{subject},</if>
            <if test="updateTime!=null"> update_time =#{updateTime},</if>
        </set>
        where id = #{id}
    </update>

</mapper>